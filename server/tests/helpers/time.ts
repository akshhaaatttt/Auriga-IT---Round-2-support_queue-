export const SECOND = 1000;
export const MINUTE = 60 * SECOND;
export const HOUR = 60 * MINUTE;

/** Fixed reference instant so every test is deterministic. */
export const NOW = Date.UTC(2026, 8, 16, 12, 0, 0);
